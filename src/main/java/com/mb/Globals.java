package com.mb;

import java.io.File;

public class Globals {
    public static final String FROM_PATH = "C:" + File.separator + "math_expr_calc" + File.separator;
    public static final String FROM_FILENAME = "expr_input.txt";
}
